// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// Any JavaScript/Coffee file within this directory, lib/assets/javascripts, vendor/assets/javascripts,
// or vendor/assets/javascripts of plugins, if any, can be referenced here using a relative path.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// compiled file.
//
// Read Sprockets README (https://github.com/sstephenson/sprockets#sprockets-directives) for details
// about supported directives.
//
//= require jquery
//= require jquery_ujs
//= require turbolinks
//= require_tree .

function collapsed() {
  // alert(data.message);
  $("#video")[0].pause();
  $("body").css({ backgroundColor: "#FF0000" });
  
  var audio = new Audio('/boom.mp3');
  audio.load();
  audio.play();
}

function awake() {
  $("#video")[0].play();
  $("body").css({ backgroundColor: "#00FF00" });
}

$(function(){
  $("#video")[0].play();
  
  $("#play").on("click", function(event){
    $("#video")[0].play();
    event.preventDefault();
  });
  
  $("#collapse").on("click", function(event){
    collapsed();
    event.preventDefault();
  });

  $("#awake").on("click", function(event){
    awake();
    event.preventDefault();
  });

  $("#videos li a").on("click", function(event){
    $("#video").attr("src", $(this).data("video-url"));
    $("#video")[0].src = $(this).data("video-url");
    $("#video")[0].load();
    $("#video")[0].play();
    event.preventDefault();
  });
  
  
  
  
  Pusher.log = function(message) {
    if (window.console && window.console.log) {
      window.console.log(message);
    }
  };

  var pusher = new Pusher('<%= Rails.application.secrets.pusher_secret %>');
  var channel = pusher.subscribe('collapse');
  channel.bind('collapsed', function(data) {
    collapsed();
  });
  
  channel.bind('awake', function(data) {
    awake();
  });
  
  $("#mode")
  
});







$(document).ready(function(){

  $(document).keydown(function (e){
    if(e.keyCode === 39) { // right
      if ($(".list-item.siliconvalley").hasClass("active")) {
        $(".list-item.siliconvalley").removeClass("active");
        $(".list-item.breakingbad").addClass("active");

        $("#meta .wrapper").removeClass("");
        $("#meta .wrapper").addClass("breakingbad");

        $("#poster-image .wrapper").removeClass("");
        $("#poster-image .wrapper").addClass("breakingbad");
      }
      else if ($(".list-item.breakingbad").hasClass("active")) {
        $(".list-item.breakingbad").removeClass("active");
        $(".list-item.dexter").addClass("active");

        $("#meta .wrapper").removeClass("");
        $("#meta .wrapper").addClass("dexter");

        $("#poster-image .wrapper").removeClass("");
        $("#poster-image .wrapper").addClass("dexter");
      }
      else if ($(".list-item.dexter").hasClass("active")) {
        $(".list-item.dexter").removeClass("active");
        $(".list-item.gameofthrones").addClass("active");

        $("#meta .wrapper").removeClass("");
        $("#meta .wrapper").addClass("gameofthrones");

        $("#poster-image .wrapper").removeClass("");
        $("#poster-image .wrapper").addClass("gameofthrones");
      }
    }

    if(e.keyCode === 37) { // left
      if ($(".list-item.gameofthrones").hasClass("active")) {
        $(".list-item.gameofthrones").removeClass("active");
        $(".list-item.dexter").addClass("active");

        $("#meta .wrapper").removeClass("");
        $("#meta .wrapper").addClass("dexter");

        $("#poster-image .wrapper").removeClass("");
        $("#poster-image .wrapper").addClass("dexter");
      }
      else if ($(".list-item.dexter").hasClass("active")) {
        $(".list-item.dexter").removeClass("active");
        $(".list-item.breakingbad").addClass("active");

        $("#meta .wrapper").removeClass("");
        $("#meta .wrapper").addClass("breakingbad");

        $("#poster-image .wrapper").removeClass("");
        $("#poster-image .wrapper").addClass("breakingbad");
      }
      else if ($(".list-item.breakingbad").hasClass("active")) {
        $(".list-item.breakingbad").removeClass("active");
        $(".list-item.siliconvalley").addClass("active");

        $("#meta .wrapper").removeClass("");
        $("#meta .wrapper").addClass("siliconvalley");

        $("#poster-image .wrapper").removeClass("");
        $("#poster-image .wrapper").addClass("siliconvalley");
      }
    }

    if(e.keyCode === 13) { // enter
      if ($(".list-item.siliconvalley").hasClass("active")) {
        if ($("#poster").hasClass("active")) {
          $("#poster").removeClass("active");
        } else {
          $("#poster").addClass("active");
        }
      }
    }
  });

});
